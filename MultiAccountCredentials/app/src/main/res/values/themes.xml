<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MultiAccountCredentials" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MultiAccountCredentials" parent="Base.Theme.MultiAccountCredentials" />

    <style name="RoundedImage">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="VeriflyActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Enable transparent status bar -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>


        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>

    </style>

    <style name="Theme.AppThemeMain" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/red_dark</item>
        <item name="android:windowLightStatusBar">false</item> <!-- Ensures text/icons are white -->
    </style>


    <style name="Theme.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">#6200EE</item>
        <item name="colorPrimaryDark">#FFFFFF</item> <!-- Status bar background color -->
        <item name="colorAccent">#03DAC5</item>

        <!-- Set status bar text color to black -->
        <item name="android:statusBarColor">#FFFFFF</item> <!-- Status bar background color -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- Navigation bar customization (optional) -->
        <!--        <item name="android:windowTranslucentStatus">true</item>-->
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="RoundImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.AppThemeMain" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="MainTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">#6200EE</item>
        <item name="colorPrimaryDark">#FFFFFF</item> <!-- Status bar background color -->
        <item name="colorAccent">#03DAC5</item>

        <!-- Set status bar text color to black -->
        <item name="android:statusBarColor">#FFFFFF</item> <!-- Status bar background color -->
        <item name="android:windowLightStatusBar">true</item> <!-- Make status bar text dark -->
    </style>

</resources>